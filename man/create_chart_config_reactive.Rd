% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_spc_chart_config.R
\name{create_chart_config_reactive}
\alias{create_chart_config_reactive}
\title{Create Chart Configuration Reactive}
\usage{
create_chart_config_reactive(
  module_data_reactive,
  column_config_reactive,
  chart_type_reactive
)
}
\arguments{
\item{module_data_reactive}{Reactive expression returning filtered data frame}

\item{column_config_reactive}{Reactive expression returning column mappings}

\item{chart_type_reactive}{Reactive expression returning chart type string}
}
\value{
Reactive expression returning list with structure:
\itemize{
\item x_col: X-axis column name (typically date)
\item y_col: Y-axis column name (typically value)
\item n_col: N-values column name for subgrouped charts
\item chart_type: Chart type string (run, i, mr, t, p, c, u, ewma, etc.)
Returns NULL if required validations fail (no data, y_col missing)
}
}
\description{
Builds a reactive expression that reads data and user selections to create
a valid chart configuration. Validates that selected columns exist in the
data and handles fallback scenarios gracefully.
}
\details{
Validation flow:
\enumerate{
\item Check data exists and is valid (data frame with rows/columns)
\item Check column config exists and is list
\item Check chart type (fallback to "run" if NULL)
\item Validate selected columns exist in data (fallback to NULL if missing)
\item Ensure y_col is available (critical requirement)
\item Return configuration or NULL
}

Uses shiny::req() for early exit and safe validation to prevent hanging
reactive chains when dependencies are incomplete.
}
\keyword{internal}
