% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config_ui_getters.R
\name{get_ui_font_scaling}
\alias{get_ui_font_scaling}
\title{Get font scaling configuration}
\usage{
get_ui_font_scaling(parameter = "divisor")
}
\arguments{
\item{parameter}{Character string naming the scaling parameter}
}
\value{
Numeric configuration value, or NULL if not found
}
\description{
Returns responsive font scaling parameters for adaptive text sizing.
These values control how base font size is calculated based on viewport dimensions.
}
\details{
\strong{Configuration parameters:}
\itemize{
\item \code{"divisor"}: Viewport diagonal divisor (lower = larger fonts) — default 42
\item \code{"min_size"}: Minimum font size in points — default 8
\item \code{"max_size"}: Maximum font size in points — default 64
}

\strong{Font scaling formula:}
\code{base_size = max(min_size, min(max_size, diagonal / divisor))}
where \code{diagonal = sqrt(width_px * height_px)}
}
\examples{
\dontrun{
# Calculate responsive base font size
divisor <- get_ui_font_scaling("divisor")
viewport_diagonal <- sqrt(800 * 600)
base_size <- max(
  get_ui_font_scaling("min_size"),
  min(
    get_ui_font_scaling("max_size"),
    viewport_diagonal / divisor
  )
)
}

}
\keyword{internal}
