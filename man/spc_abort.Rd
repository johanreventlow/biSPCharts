% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_error_handling.R
\name{spc_abort}
\alias{spc_abort}
\title{Typed error helper for SPC pipeline}
\usage{
spc_abort(message, class, ..., call = rlang::caller_env())
}
\arguments{
\item{message}{Character string. Dansk bruger-vendt fejlbesked.}

\item{class}{Character string. One of \code{"spc_input_error"}, \code{"spc_prepare_error"},
\code{"spc_render_error"}, \code{"spc_cache_error"}.}

\item{...}{Additional metadata passed to \code{rlang::abort()} (e.g. \code{data}, \code{column}).}

\item{call}{Environment. Defaults to caller environment for correct traceback.}
}
\value{
Does not return — always throws.
}
\description{
Throws a typed condition that inherits from \code{spc_error} and \code{error}.
Domain helpers use this instead of \code{stop()} so callers can distinguish
failure modes and the orchestrator can catch by class.
}
\section{Error classes}{

\itemize{
\item \code{spc_input_error}: Ugyldig input (forkert chart_type, manglende kolonner).
\item \code{spc_prepare_error}: Data-prep fejl (dato-/talparse, filtrering).
\item \code{spc_render_error}: BFHcharts-fejl under rendering.
\item \code{spc_cache_error}: Cache-læs/skriv fejl (typisk warn, ikke error).
}

All classes inherit from \code{"spc_error"} → generic \code{tryCatch(error = ...)} still works.
}

\keyword{internal}
